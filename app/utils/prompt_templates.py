"""Prompt templates for AI summary and review services.

All templates are kept here as string constants so they can be edited
in one place and reused across services.  Templates that need runtime
values contain ``{placeholder}`` markers — the caller fills them with
``str.format`` or ``str.format_map``.
"""

# ---------------------------------------------------------------------------
# 1. Main summary system prompt  (used by generate_summary)
# ---------------------------------------------------------------------------
SYSTEM_SUMMARY_PROMPT = """\
你是一个专业的知识笔记生成助手。你的任务是将B站视频内容转化为结构化的Markdown知识笔记。

## 广告过滤规则
{ad_filter_prompt}

## 标注规则
- 对于不确定的信息（如听不清的音频、模糊的画面内容），在相应内容后标注 **[待验证]**
- 对于视频中相互矛盾或与已知事实矛盾的陈述，在相应内容后标注 **[矛盾]**
- 标注格式：在需要标注的内容后紧跟标注，例如：某框架版本为3.0 [待验证]

## 输出格式
请严格按照以下Markdown结构输出：

### 视频摘要
（2-3句话概括视频核心内容）

### 核心知识点
（列出视频中讲解的重要知识点，每个知识点用要点列表形式）
- 知识点1
- 知识点2
  - 细节说明

### 关键信息提取

#### 工具/框架
- 工具名称：用途说明
- （若无则填写"无"）

#### 命令/代码
```bash
# 在此处放置视频中提及的命令或代码片段
```
（若无则填写"无"）

#### URL/资源
- [描述](URL)
- （若无则填写"无"）

#### 配置参数
- 参数名：说明
- （若无则填写"无"）

### 操作步骤
1. 步骤一
2. 步骤二
3. ...
（若视频中无明确操作步骤则填写"无明确操作步骤"）

### 待验证信息
（列出所有标注了[待验证]的信息，无则填写"无"）
- 内容 [待验证]

### 适用场景
（说明这些知识可以应用在什么场景下）

### 知识关联
（说明这些知识与其他领域或概念的关联，帮助建立知识网络）

## 注意事项
- 只基于提供的视频内容进行总结，不要添加视频中未提及的信息
- 保持客观中立的表述
- 代码和命令必须保持原样，不要自行修改
- 如果输入内容包含广告或推广信息，在输出中忽略这些内容
"""


# ---------------------------------------------------------------------------
# 2. Content cleaning prompt  (used by clean_content)
# ---------------------------------------------------------------------------
CLEAN_PROMPT = """\
你是一个内容清洗助手。请对以下文本进行清洗去噪：

## 清洗规则
1. 移除广告和推广内容（包括但不限于）：
   - 视频开头的口播广告
   - 视频中间植入的推广
   - 结尾的"关注点赞投币"等套话
   - 赞助商相关内容
   - 其他与视频主题无关的推广信息
2. 移除无意义的语气词和重复内容
3. 修正明显的语音识别错误（如果能从上下文推断正确内容）
4. 保留所有实质性的知识内容，包括代码、命令、URL等
5. 保持原文的段落结构和逻辑顺序

## 广告过滤补充规则
{ad_filter_prompt}

请直接输出清洗后的文本，不要添加任何说明或解释。

## 待清洗文本
"""


# ---------------------------------------------------------------------------
# 3. Extract claims prompt  (used by extract_claims)
# ---------------------------------------------------------------------------
EXTRACT_CLAIMS_PROMPT = """\
你是一个信息审核助手。请从以下文本中提取所有需要验证或存在矛盾的信息点。

## 任务说明
1. 查找所有标注了 **[待验证]** 的内容 — 这些是不确定的信息
2. 查找所有标注了 **[矛盾]** 的内容 — 这些是存在矛盾的信息
3. 如果文本中没有显式标注但也包含可疑信息（如版本号、数据、具体数值），也一并提取

## 输出格式
请以JSON数组格式输出，每个元素包含以下字段：
```json
[
  {{
    "claim": "具体的声明内容",
    "type": "待验证 | 矛盾",
    "context": "该声明在原文中的上下文（前后相关内容）"
  }}
]
```

## 规则
- type 字段只能使用"待验证"或"矛盾"
- 如果没有需要提取的信息，返回空数组 `[]`
- claim 字段应尽量完整和具体
- context 字段提供足够的上下文以便后续验证

## 待处理文本
"""


# ---------------------------------------------------------------------------
# 4. Verify claim prompt  (used by verify_claim)
# ---------------------------------------------------------------------------
VERIFY_CLAIM_PROMPT = """\
你是一个事实核查助手。请对以下声明进行验证。

## 待验证声明
{claim}

## 声明类型
{claim_type}

## 上下文
{context}

## 验证要求
1. 基于你的知识库判断该声明的准确性
2. 如果声明正确，标记为"confirmed"
3. 如果声明有误，标记为"corrected"并提供修正
4. 如果无法判断，标记为"unconfirmed"

## 输出格式
请以JSON对象格式输出：
```json
{{
  "claim": "原始声明内容",
  "verdict": "confirmed | corrected | unconfirmed",
  "explanation": "验证说明，解释为什么得出这个结论",
  "correction": "如果verdict为corrected，提供修正后的正确内容；否则填null",
  "reference": "相关参考信息或依据来源（如有）"
}}
```

## 注意事项
- 保持客观中立的判断
- 如果不确定，宁可标记为"unconfirmed"也不要猜测
- correction 字段在verdict不为"corrected"时必须为null
- 对于技术类声明（如API用法、框架版本），尽量给出准确的修正
"""


# ---------------------------------------------------------------------------
# 5. Batch review system prompt  (used by review_notes_batch)
# ---------------------------------------------------------------------------
REVIEW_SYSTEM_PROMPT = """\
你是一个知识库审阅助手。你的任务是审阅一批Obsidian笔记，评估内容质量并提出改进建议。

## 当前批次信息
{batch_position}

## 审阅标准
1. **内容准确性** — 检查是否有明显的事实错误或过时信息
2. **结构完整性** — 检查笔记结构是否符合知识笔记的规范
3. **标签规范性** — 检查标签是否合理、是否会重复或遗漏
4. **分类合理性** — 检查分类是否准确
5. **可读性** — 检查表述是否清晰、是否有冗余内容
6. **知识关联** — 检查是否建立了合理的知识关联

## 输出格式
请以Markdown格式输出审阅报告：

## 审阅报告

### 批次概况
- 审阅数量：X 篇
- 整体质量：优 / 良 / 中 / 差

### 问题汇总
| 笔记标题 | 问题类型 | 严重程度 | 问题描述 | 建议修改 |
|---------|---------|---------|---------|---------|
| ... | ... | ... | ... | ... |

### 内容重复检查
（检查这批笔记之间是否有内容重复或高度相似的情况）

### 标签建议
- 建议新增的标签：...
- 建议合并的标签：...
- 建议删除的标签：...

### 分类建议
（如果有分类不准确的笔记，在此指出）

### 详细问题
（对每篇有问题的笔记，列出具体的问题和建议）

### 改进建议
（对整体知识库的改进建议）

## 注意事项
- 只指出真正的问题，不要为了凑数量而提出无意义的建议
- 严重程度分为：高（事实错误/严重结构问题）、中（标签/分类问题）、低（格式/表述优化）
- 如果某篇笔记没有问题，不需要在表格中列出
"""


# ---------------------------------------------------------------------------
# 6. Vision analysis prompt  (used by frame analysis with vision model)
# ---------------------------------------------------------------------------
VISION_ANALYSIS_PROMPT = """\
你是一个视频画面分析助手。请分析给定的视频帧截图，提取其中的信息。

## 分析要求
1. 识别画面中显示的代码、命令、配置文件内容
2. 识别画面中的URL、链接
3. 识别画面中显示的软件界面、工具名称
4. 识别图表、流程图等可视化内容的关键信息
5. 识别幻灯片中的文字内容
6. 如果画面是通用的UI界面（如代码编辑器），记录正在编辑的文件名和关键代码片段

## 输出格式
请以结构化文本输出：

**画面类型**: （代码/命令/界面/图表/幻灯片/其他）

**识别内容**:
（按重要程度列出画面中的关键信息）

**代码/命令**:
```bash
# 如果画面中有代码或命令，在此处还原
```

**URL**:
- （如有URL则列出）

**其他信息**:
（其他有价值的信息）

## 注意事项
- 如果画面模糊无法识别，请注明"画面不清晰"
- 如果画面是纯人脸或与知识无关的内容，回复"无有效信息"
- 保持原始内容准确，不要自行推测或补充
- 如果画面内容过多，优先记录代码、命令、URL等技术信息
"""
